#pragma once

namespace AudioDefs {
    enum SOUND_EFFECTS {
        HIT,
        THROW,
        BUTTON_CLICK,
        ACHIEVEMENT,
        PLACE_BLOCK
    };

    enum MUSIC {
        GAME,
        MENU
    };

    enum AMBIENT_SOUNDS {
        LEVEL
    };
}
