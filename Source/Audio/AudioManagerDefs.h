#pragma once

namespace AudioDefs {
    enum SOUND_EFFECTS {
        HIT,
        THROW,
        BUTTON_CLICK,
        ACHIEVEMENT
    };

    enum MUSIC {
        GAME,
        MENU
    };

    enum AMBIENT_SOUNDS {
        LEVEL
    };
}