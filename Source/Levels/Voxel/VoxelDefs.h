#pragma once

enum BlockSide {
    TOP,
    BOTTOM,
    LEFT,
    RIGHT,
    FRONT,
    BACK
};

enum BlockType {
    AIR,
    STONE,
    DIRT,
    SAND,
    COAL,
    NONE
};

enum ChunkType {
    SKY,
    TERRAIN,
    GROUND
};